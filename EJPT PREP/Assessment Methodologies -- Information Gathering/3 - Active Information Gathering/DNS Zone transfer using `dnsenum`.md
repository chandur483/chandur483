`dnsenum` is a higher-level tool that automates DNS discovery: it queries NS, attempts AXFR, enumerates subdomains (wordlists), checks whois, and can query other sources. It’s convenient when you want a one-command sweep.

Basic usage (common form):
```
dnsenum --enum example.com
```

