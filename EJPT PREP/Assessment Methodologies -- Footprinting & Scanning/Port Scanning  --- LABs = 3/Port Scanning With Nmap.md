
nmap with noroot it uses  TCP
nmap with root uses SYN

-Pn for no host discovery directly port scanning
-F is fast scans most use port
default nmap scan first 1000 ports
-sn ping sweep with ICMP
-sn -PS for ping sweep with SYN
-T4 for speed
-v verbose
-sU UDP